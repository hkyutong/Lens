# Deprecated

This document is no longer used in the Lens fork.

`academic-4.0` runs as an API service and should be started with:

```bash
python main.py
```

See:

- `../README.md`
- `../changelogs.md`
